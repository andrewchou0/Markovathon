# Backend — Python 3.11
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2

# Storage — local MongoDB (mongodb://localhost:27017), sync driver
pymongo==4.9.1

# Dev only: validating seed JSON against /contracts/*.schema.json
jsonschema==4.23.0
